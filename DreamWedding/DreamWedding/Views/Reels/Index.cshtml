@model IEnumerable<DreamWedding.Models.Reels>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReelsVideo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Captions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReelsVideo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Captions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReelsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReelsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReelsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
